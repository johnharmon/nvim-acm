-- Auto-loaded entry point. Users can call require('autoshift').setup({...})
-- explicitly; this file just makes the plugin discoverable in &runtimepath.
if vim.g.autoshift_loaded then return end
vim.g.autoshift_loaded = true
