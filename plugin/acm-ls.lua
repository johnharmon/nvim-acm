-- Auto-loaded entry point. Users can call require('acm-ls').setup({...})
-- explicitly; this file just makes the plugin discoverable in &runtimepath.
if vim.g.acm_ls_loaded then return end
vim.g.acm_ls_loaded = true
